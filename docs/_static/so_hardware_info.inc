.. code-block:: console

    usage: so_hardware_info [hardware file, [hardware_file]] ...

    This program reads a hardware model and prints some summary text to the
    terminal.

    positional arguments:
      hardware    Input hardware file

    optional arguments:
      -h, --help  show this help message and exit
